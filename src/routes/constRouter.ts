import { Router, Request, Response } from "express";
import { setDatabaseConnection } from "../middleware/setDatabase";

const constRouter = Router()

constRouter.get("/category/get",(req: Request, res: Response) => {
    
})